services:
  postgres:
    image: postgres:16
    container_name: smartstore-db
    environment:
      POSTGRES_DB: smartstore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - smartstore_data:/var/lib/postgresql/data

volumes:
  smartstore_data: